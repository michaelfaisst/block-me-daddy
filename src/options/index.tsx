import React from "react";
import ReactDOM from "react-dom/client";
import OpionsPage from "./page";
import "../index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <OpionsPage />
    </React.StrictMode>
);
